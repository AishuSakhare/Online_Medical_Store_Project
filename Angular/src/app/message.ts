export class Message {
    constructor(
        public id: number,
        public messageId: number,
        public messageType: string,
        public email: string,
        public message: string
  
    ) { }
}
